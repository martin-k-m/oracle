# twill build artifacts and local scratch
*.tmp
.DS_Store

# checkpoints are committed deliberately (small); ignore stray ones
models/*.bin.tmp

# test scratch
tests/*.tmp.bin
models/test-*.bin

# GPT-2 open-weights: downloaded and converted locally, never committed
models/gpt2/
models/qwen*/
scripts/__pycache__/
models/embed/
